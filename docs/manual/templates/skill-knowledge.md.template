---
name: {{skill_name}}
description: {{业务领域}}本体知识与工具使用策略
type: domain_knowledge
---

# {{业务领域}}知识

## 本体（{{N}} Object / {{M}} Link）
- **Object**: {{Object1}}、{{Object2}}、...
- 读实体用 `query_entity`，遍历关系用 `traverse_relation`。
{{#若该 vertical 有专属读工具}}
- 专属查询用 `{{query_tool_name}}`。
{{/}}

## 单一事实源规则
{{#如本 vertical 有类似折扣的数值规则}}
{{规则名}}由 {{字段}} 决定，具体数值见本体数据 `data/{{vertical_name}}/{{rules_file}}.json`。
**不要在本对话中重复定义数值**。
{{/}}

## 状态
- {{Object}}.status：{{状态1}} / {{状态2}} / ...
状态迁移只能经 Action，不可直接改写。
