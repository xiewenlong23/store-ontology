<!-- BILINGUAL -->
> Source: https://www.palantir.com/docs/foundry/object-explorer/overview/
---
# Object Explorer
**Object Explorer** 是一个用于回答 Ontology 中任何相关问题的搜索和分析工具。

**Object Explorer** is a search and analysis tool for answering questions about anything in the Ontology.
* 用户可以通过运行从简单关键字搜索到全面 Property 过滤器的各种查询，轻松找到感兴趣的对象，所有操作都通过直观的点选式 Interface 完成。之后，他们可以使用探索视图（exploration view）查看生成的对象集，或将它们视为结果表格，或选择特定对象以查看其 [Object View](/docs/foundry/object-views/overview/)。

* 此外，用户可以比较和对比对象集，对对象集执行批量 Actions（例如 writeback），并在其他兼容的应用程序（例如 Quiver）中打开对象集，或将其导出。用户还可以保存其探索结果，并随时重新访问以查看最新结果。

* 探索视图是一组预设和可配置的可视化（例如图表或地图），用户可以进一步利用这些可视化深入到特定的 Object 子集中。

* Object Explorer 几乎不需要配置，专注于面向非技术用户。

* Users can easily find objects of interest by running queries ranging from simple keyword searches to comprehensive property filters, all from an intuitive point-and-click interface. From there, they can explore the resulting object sets using the exploration view, or view them as a table of results, or select a specific object to see its [Object View](/docs/foundry/object-views/overview/).
* Additionally, users can compare and contrast object sets, take bulk Actions (like writeback) on the object set, and open the object set in other compatible applications (such as Quiver), or export them. Users can also save their Explorations, and revisit them at any time to see the latest results.
* The exploration view is a set of preset and configurable visualizations (such as charts or maps) that the user can further leverage to drill-down into specific subsets of Objects.
* Object Explorer requires minimal configuration, and is geared towards less technical users.
通过 [Object Explorer Interface 概述](/docs/foundry/object-explorer/getting-started/) 开始了解。

Get started by walking through an [overview of the Object Explorer interface](/docs/foundry/object-explorer/getting-started/).
您还可以了解有关 [对象及基于 Ontology 的数据建模背后的概念](/docs/foundry/ontology/core-concepts/) 的更多信息。

You can also learn more about [objects and the concepts behind ontology-based data modeling](/docs/foundry/ontology/core-concepts/).
或者，查看 [如何为单个 Object 配置 Object View](/docs/foundry/object-views/overview/)。

Or, take a look at [how to configure Object Views for individual Objects](/docs/foundry/object-views/overview/).