# ============================================================
# requirements.txt — Phase 2-7 完整依赖
# ============================================================
# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Agent / LangChain
copilotkit>=0.1.88
langgraph>=0.2.0
langchain-core>=0.3.0

# 本体论 / 图数据库
rdflib>=7.0.0

# 可观测性 / 审计
structlog>=24.0.0                     # ⭐ Phase 2 新增（结构化日志）
asyncpg>=0.29.0                       # ⭐ Phase 5 新增（PostgreSQL 异步驱动）
aiofiles>=23.0.0                      # ⭐ Phase 5 新增（审计日志文件写入）

# MCP
langchain-mcp-adapters>=0.1.0        # ⭐ Phase 6 新增（MCP Server 接入）

# 测试
pytest>=8.0.0                         # ⭐ Phase 7 新增
pytest-asyncio>=0.23.0                # ⭐ Phase 7 新增（异步测试）
